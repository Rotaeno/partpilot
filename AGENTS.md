# PartPilot

个人作品：基于工业配件 PRD 的单用户配件查找助手。所有写入、依赖、日志、Git 操作仅限本目录；上级资料只读。不要复制企业数据或密钥。

恢复时先读本文件、`docs/PLAN.md`、`docs/STATUS.md`，再检查 `git status` 和实现。以实际测试证据为准。

技术：Python / LangGraph / FastAPI / SQLite，原生 HTML/CSS/JS。默认离线规则演示，Qwen 适配器可配置；本次 API 预算为 0 元，禁止真实外部模型请求。模拟通过不等于真实模型通过。

必须：缺设备追问、显式检索、条件更改使旧候选失效、真实数据库查询、无结果诊断、显式确认且幂等保存、会话恢复、工具异常处理。模型只提出动作和字段变更；业务规则和写入由代码把关。

每阶段更新 STATUS；定位→修复→针对性验证。同故障三次无进展则重新诊断。不能删失败测试、改低验收或硬编码演示答案。完成冻结的验收即交付，不扩范围。

验证：`.venv/Scripts/python -m pytest -q`、`scripts/verify.ps1`、真实浏览器核心流程和截图。日志/截图在 `artifacts/`；运行库在 `runtime/`，均不提交。不要提交 `.env`、密钥、`.venv`。依赖缓存和临时目录也必须在项目内。
